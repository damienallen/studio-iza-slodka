http://localhost:8000 {
  # tls self_signed
  # header / Strict-Transport-Security "max-age=31536000;"

  # Serve static files:
  root /var/www

  # Allows to use `.gz` files when available:
  gzip

  # Logs:
  log stdout
  errors stdout
}
